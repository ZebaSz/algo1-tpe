\begin{problema}{recargarProductos}{s: Sistema, id: Id, ps: [Producto]}{}
\requiere{(\exists d \in enjambreDrones(s)) id (d) == id \ \land \ posicionActual (d) == posicionGranero (campo (s)) \\ \land \ \neg enVuelo(d) \ \land \ bateria (d) == 100}
\asegura[mismoCampo]{campo(\pre{s}) == campo(s)}
\asegura[mismoEstadoCultivo]{(\forall i \in [0..\prm{dimensiones(campo(s))})) (\forall j \in [0..\sgd{dimensiones(campo(s))})) \\ contenido(campo(s), i, j) \neq Cultivo \lor estadoDelCultivo(s,i,j) == estadoDelCultivo(\pre{s}, i, j))}
\asegura [losDronesEstan] {(\forall d \in enjambreDrones(\pre{s}), id(d) \neq id) \\ (\exists e \in enjambreDrones(s)) mismoDrone(d,e)}
\asegura [losDronesTienenQueEstar] {(\forall d \in enjambreDrones(s), id(d) \neq id) (\exists e \in enjambreDrones(\pre{s})) \\ mismoDrone(d,e)}
\asegura {\longitud {enjambreDrones(s)} == \longitud {enjambreDrones (\pre{s})}}
\asegura {(\exists d \in enjambreDrones(s))(\exists e \in enjambreDrones(\pre{s})) id(d) == id(e) \ \land \ id(d) == id \ \land \\ losNuevosProductosDeLaLista(d,e,ps) \ \land \ losProductosSumanMenosDe100 (d)}

\end{problema}

\begin{problema}{vuelanEnFormacion}{s: Sistema}{Bool}
\requiere{(\forall d \in enjambreDrones(s)) \longitud{vueloRealizado(d)} \geq 3}
\requiere {\longitud {enjambreDrones(s)} \geq 2}
\asegura {res == (\longitud {enjambreDrones(s)} < \longitud {dronesEnFormacion(s)} \times 2)}
\end{problema}

\begin{problema}{droneAward}{s: Sistema}{Drone}
\requiere{\longitud {enjambreDrones(s)} \geq 1}
\asegura {res == menorId(menosProductos(masParcelas(s)))}
\end{problema}